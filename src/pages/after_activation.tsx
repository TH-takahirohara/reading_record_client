import { AfterActivationContainer } from '@/features/after_activation/after_activation_container';
import type { NextPage } from 'next';

const AfterActivationPage: NextPage = () => <AfterActivationContainer />;

export default AfterActivationPage;
