import { ActivationContainer } from '@/features/activation/activation_container';
import type { NextPage } from 'next';

const ActivationPage: NextPage = () => <ActivationContainer />;

export default ActivationPage;
