import { RegisterContainer } from '@/features/register/register_container';
import { NextPage } from 'next';

const RegisterPage: NextPage = () => <RegisterContainer />;

export default RegisterPage;
